const User = require('./User');
const Prediction = require('./Prediction');
const BlockedIP = require('./BlockedIP');
const Alert = require('./Alert');

module.exports = { User, Prediction, BlockedIP, Alert };
